---
status: accepted
---

# Keep browser Print out of the export workflow

Direct browser Print bypasses clean preview, Version identity, atomic history persistence, and exact re-export, so it will not be a user-facing export path; Cmd/Ctrl+P routes into Export instead. The print Export Adapter remains as an internal reference and testing path while product export produces explicit PDF or DOCX artifacts from the same stored Layout Plans.
