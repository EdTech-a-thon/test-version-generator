"use client";

import Link from "next/link";
import { FormEvent, useDeferredValue, useEffect, useState } from "react";
import { VariantPreview } from "@/components/variant-preview";
import { AppHeader } from "@/components/app-header";
import type { ParametricDefinition, VariableDefinition } from "@/lib/contracts";

const starter: ParametricDefinition = { variables: [{ name: "m", type: "integer", min: 2, max: 10 }, { name: "a", type: "integer", min: 1, max: 9 }], constraints: ["m != a"], answerFormula: "m * a", distractors: ["m + a", "m - a", "m / a"], unit: "N", rounding: { mode: "decimals", value: 2 } };

function FormulaEditor({ definition, onChange }: { definition: ParametricDefinition; onChange: (definition: ParametricDefinition) => void }) {
  function updateVariable(index: number, field: keyof VariableDefinition, value: string) {
    onChange({ ...definition, variables: definition.variables.map((variable, current) => current === index ? { ...variable, [field]: field === "name" ? value.replace(/[^a-zA-Z0-9_]/g, "") : Number(value) } : variable) });
  }
  return <section className="formula-editor"><div><p className="eyebrow">Changing values</p><h2>Set up the values</h2><p className="helper">Use a value in the question stem with double curly brackets, such as <code>{"{{m}}"}</code>.</p></div>{definition.variables.filter((value): value is VariableDefinition => value.type !== "dataset").map((variable, index) => <div className="variable-row" key={index}><strong>Value {index + 1}</strong><label>Name<input value={variable.name} onChange={(event) => updateVariable(index, "name", event.target.value)} /></label><label>Smallest<input type="number" value={variable.min ?? ""} onChange={(event) => updateVariable(index, "min", event.target.value)} /></label><label>Largest<input type="number" value={variable.max ?? ""} onChange={(event) => updateVariable(index, "max", event.target.value)} /></label>{definition.variables.length > 1 && <button type="button" className="text-button" onClick={() => onChange({ ...definition, variables: definition.variables.filter((_, current) => current !== index) })}>Remove</button>}</div>)}{definition.variables.length < 4 && <button type="button" className="secondary" onClick={() => onChange({ ...definition, variables: [...definition.variables, { name: "x", type: "integer", min: 1, max: 10 }] })}>Add a changing value</button>}<label>Correct answer calculation<input value={definition.answerFormula} onChange={(event) => onChange({ ...definition, answerFormula: event.target.value })} placeholder="For example: m * a" /></label><p className="helper">Use <code>*</code> for multiply and <code>/</code> for divide.</p><fieldset><legend>Likely incorrect calculations</legend>{definition.distractors.map((distractor, index) => <label key={index}>Wrong answer {index + 1}<input value={typeof distractor === "string" ? distractor : ""} onChange={(event) => onChange({ ...definition, distractors: definition.distractors.map((value, current) => current === index ? event.target.value : value) })} /></label>)}</fieldset><div className="two-col"><label>Answer unit<input value={definition.unit ?? ""} onChange={(event) => onChange({ ...definition, unit: event.target.value })} placeholder="For example: N" /></label><label>Decimal places<select value={definition.rounding.value} onChange={(event) => onChange({ ...definition, rounding: { mode: "decimals", value: Number(event.target.value) } })}>{[0, 1, 2, 3].map((value) => <option value={value} key={value}>{value}</option>)}</select></label></div><label>Optional rule<textarea value={definition.constraints[0] ?? ""} onChange={(event) => onChange({ ...definition, constraints: event.target.value.trim() ? [event.target.value] : [] })} rows={2} placeholder="For example: m != a" /></label></section>;
}

export default function NewQuestion() {
  const [parametric, setParametric] = useState(false); const [definition, setDefinition] = useState(starter); const deferredDefinition = useDeferredValue(definition); const [message, setMessage] = useState(""); const [saving, setSaving] = useState(false); const [banks, setBanks] = useState<Array<{ id: string; name: string }>>([]); const [bankId, setBankId] = useState("");
  useEffect(() => { void fetch("/api/banks").then((response) => response.json()).then((data) => { if (Array.isArray(data)) { setBanks(data); setBankId(data[0]?.id ?? ""); } }); }, []);
  async function save(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setSaving(true); setMessage(""); const values = new FormData(event.currentTarget); const options = ["A", "B", "C", "D"].map((id) => ({ id, text: String(values.get(`option-${id}`) ?? "") })).filter((option) => option.text.trim()); const correct = String(values.get("correct") ?? ""); const response = await fetch("/api/questions", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ bankIds: bankId ? [bankId] : [], type: "MULTIPLE_CHOICE", stem: values.get("stem"), options, correctAnswer: correct ? [correct] : [], difficulty: Number(values.get("difficulty")), points: Number(values.get("points")), tags: String(values.get("tags")).split(",").map((tag) => tag.trim()).filter(Boolean), solution: values.get("solution"), parametric: parametric ? definition : undefined }) }); const data = await response.json(); setMessage(response.ok ? "Question saved. It is ready to use in a test." : data.error ?? "Could not save question."); setSaving(false); }
  return <main><AppHeader current="Author a question" /><section className="workspace"><div className="section-heading"><div><p className="eyebrow">Step 2 of 3: Build your library</p><h1>Author a question</h1><p className="lead">Add a reusable multiple-choice question, then use it in any test you create.</p></div><Link className="quiet-link" href="/banks">Back to question banks</Link></div>{message && <p className="notice" role="status">{message} {message.startsWith("Question saved") && <Link href="/tests/new">Create a test</Link>}</p>}<form onSubmit={save} className="editor-grid"><section className="editor"><label>Question bank<select value={bankId} onChange={(event) => setBankId(event.target.value)} required><option value="">Choose a bank</option>{banks.map((bank) => <option key={bank.id} value={bank.id}>{bank.name}</option>)}</select></label>{!banks.length && <p className="warning">Create a <Link href="/banks">question bank</Link> before saving a question.</p>}<label>Question stem<textarea name="stem" defaultValue="A mass of {{m}} kg accelerates at {{a}} m/s². Net force?" rows={4} required /></label><div className="two-col"><label>Difficulty<select name="difficulty" defaultValue="3">{[1, 2, 3, 4, 5].map((value) => <option key={value}>{value}</option>)}</select></label><label>Points<input name="points" defaultValue="1" type="number" min="0.5" step="0.5" required /></label></div><label>Tags<input name="tags" defaultValue="mechanics" placeholder="For example: mechanics, forces" /><span className="field-help">Separate tags with commas. Tests can use a tag to select the right questions.</span></label><fieldset><legend>Answer choices</legend><p className="field-help">Choose the correct answer using the circle beside it.</p>{["A", "B", "C", "D"].map((choice, index) => <label className="choice-input" key={choice}><input type="radio" name="correct" value={choice} defaultChecked={index === 0} aria-label={`Correct answer ${choice}`} /><span>{choice}</span><input name={`option-${choice}`} defaultValue={["m × a", "m + a", "m − a", "m ÷ a"][index]} required /></label>)}</fieldset><label>Solution<textarea name="solution" placeholder="Explain the reasoning shown in the answer key." rows={3} /></label><label className="toggle"><input type="checkbox" checked={parametric} onChange={(event) => setParametric(event.target.checked)} /> Make this a question with changing values</label>{parametric && <FormulaEditor definition={definition} onChange={setDefinition} />}<button className="primary" disabled={!banks.length || saving}>{saving ? "Saving question..." : "Save question"}</button></section><aside className="preview"><p className="eyebrow">Before you save</p><h2>A quick check</h2><ol className="checklist"><li>Choose the bank where this belongs.</li><li>Mark the correct answer.</li><li>Add a tag if you want to pull this into a test later.</li></ol><p className="helper">Changing values are optional. Turn them on when each student form needs different numbers with the same skill.</p></aside>{parametric && <VariantPreview definition={deferredDefinition} />}</form></section></main>;
}
